services:
  - type: web
    name: movie-suggest
    runtime: python
    repo: https://github.com/arsalan-ai-dev/Movie-Suggest
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app.py --server.port $PORT
    envVars:
      - key: TMDB_API_KEY
        sync: false